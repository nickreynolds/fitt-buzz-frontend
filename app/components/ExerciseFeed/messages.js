/*
 * ExerciseFeed Messages
 *
 * This contains all the text for the ExerciseFeed component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ExerciseFeed';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ExerciseFeed component!',
  },
});
