/*
 * WeightTypeModalComponent Messages
 *
 * This contains all the text for the WeightTypeModalComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.WeightTypeModalComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WeightTypeModalComponent component!',
  },
});
