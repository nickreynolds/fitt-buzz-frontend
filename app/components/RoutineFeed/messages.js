/*
 * RoutineFeed Messages
 *
 * This contains all the text for the RoutineFeed component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RoutineFeed';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RoutineFeed component!',
  },
});
