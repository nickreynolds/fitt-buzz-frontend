/*
 * RoutinePage Messages
 *
 * This contains all the text for the RoutinePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RoutinePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RoutinePage container!',
  },
});
